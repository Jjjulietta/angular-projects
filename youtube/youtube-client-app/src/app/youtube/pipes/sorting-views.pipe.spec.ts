import { SortingViewsPipe } from './sorting-views.pipe';

describe('SortingViewsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortingViewsPipe();
    expect(pipe).toBeTruthy();
  });
});
