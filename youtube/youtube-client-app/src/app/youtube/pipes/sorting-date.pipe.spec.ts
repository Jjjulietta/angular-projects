import { SortingDatePipe } from './sorting-date.pipe';

describe('SortingDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortingDatePipe();
    expect(pipe).toBeTruthy();
  });
});
